const App = () => {
  return (
    <div>
      <h1>Hello, React!</h1>
      <p>This is a basic React app written in Notepad.</p>
    </div>
  );
};

ReactDOM.createRoot(document.getElementById("root")).render(<App />);
